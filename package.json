{
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "start": "node .output/server/index.mjs",
    "dev": "nuxi dev",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "prod": "nuxi generate && nuxi start"
  },
  "devDependencies": {
    "nuxt": "^3.6.1"
  }
}
